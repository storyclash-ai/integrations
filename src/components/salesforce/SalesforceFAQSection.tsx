import{ChevronDown}from'lucide-react';
import{useState}from'react';
export default function SalesforceFAQSection(){
  const[openIndex,setOpenIndex]=useState<number|null>(null);
  const faqs=[{question:'How does Storyclash integrate with Salesforce?',answer:'Connect via OAuth to sync creator profiles as Salesforce accounts or contacts. Map influencer metrics to custom fields and track campaigns as opportunities.'},{question:'What creator data syncs to Salesforce?',answer:'Influencer profiles become accounts with custom fields including audience size, engagement rate, niche, and contact info. Campaign data syncs to opportunities and custom objects.'},{question:'Can I trigger Salesforce workflows?',answer:'Yes - set up Process Builder flows and workflows triggered by creator milestones, campaign completion, or performance thresholds reaching specific values.'},{question:'How do I track influencer ROI in Salesforce?',answer:'Link campaigns to opportunities with revenue attribution. Build Salesforce reports showing creator-driven pipeline, closed deals, and revenue contribution.'}];
  return <section className="py-20 px-6 bg-white"><div className="max-w-4xl mx-auto"><h2 className="text-4xl lg:text-5xl font-black text-gray-900 text-center mb-16">Salesforce Integration FAQ</h2><div className="space-y-4">{faqs.map((faq,idx)=><div key={idx} className="border-2 rounded-2xl overflow-hidden" style={{borderColor:'#E5EAEC'}}><button onClick={()=>setOpenIndex(openIndex===idx?null:idx)} className="w-full flex items-center justify-between p-6 text-left hover:bg-gray-50 transition-colors duration-200"><h3 className="text-lg font-black text-gray-900 pr-4">{faq.question}</h3><ChevronDown className={`w-5 h-5 flex-shrink-0 transition-transform duration-200 ${openIndex===idx?'rotate-180':''}`} style={{color:'#00A1E0'}}/></button>{openIndex===idx&&<div className="px-6 pb-6"><p className="text-gray-600 leading-relaxed">{faq.answer}</p></div>}</div>)}</div></div></section>;
}
