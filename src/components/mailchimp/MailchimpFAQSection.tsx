import{ChevronDown}from'lucide-react';
import{useState}from'react';
export default function MailchimpFAQSection(){
  const[openIndex,setOpenIndex]=useState<number|null>(null);
  const faqs=[{question:'How does Storyclash integrate with Mailchimp?',answer:'Sync creator audience segments to Mailchimp lists via API. Subscribers are automatically tagged with creator source data for segmentation and personalized campaigns.'},{question:'Can I sync creator audiences to Mailchimp?',answer:'Yes - export influencer audience segments as Mailchimp lists or add them to existing lists with creator source tags for targeted email marketing.'},{question:'How do I segment by creator source?',answer:'Storyclash tags Mailchimp subscribers with creator ID, campaign name, and engagement level. Build segments in Mailchimp based on these tags for personalization.'},{question:'Can I track email performance by creator?',answer:'Yes - use Mailchimp reports filtered by creator tags to see which influencers drive the highest email engagement, clicks, and conversions.'}];
  return <section className="py-20 px-6 bg-white"><div className="max-w-4xl mx-auto"><h2 className="text-4xl lg:text-5xl font-black text-gray-900 text-center mb-16">Mailchimp Integration FAQ</h2><div className="space-y-4">{faqs.map((faq,idx)=><div key={idx} className="border-2 rounded-2xl overflow-hidden" style={{borderColor:'#E5EAEC'}}><button onClick={()=>setOpenIndex(openIndex===idx?null:idx)} className="w-full flex items-center justify-between p-6 text-left hover:bg-gray-50 transition-colors duration-200"><h3 className="text-lg font-black text-gray-900 pr-4">{faq.question}</h3><ChevronDown className={`w-5 h-5 flex-shrink-0 transition-transform duration-200 ${openIndex===idx?'rotate-180':''}`} style={{color:'#FFE01B'}}/></button>{openIndex===idx&&<div className="px-6 pb-6"><p className="text-gray-600 leading-relaxed">{faq.answer}</p></div>}</div>)}</div></div></section>;
}
